// start /components/check.detail.sign/

//- push style and script file to the end of the page
if componentsArray
  - componentsArray[ componentsArray.length ] = 'check.detail.sign'
else
  - var componentsArray = [ 'check.detail.sign' ]

script( src="/components/check.detail.sign/storeFiles.js" )
script( src="/components/check.detail.sign/bx.ajax.runAction.js" )

script( src="https://cdn.jsdelivr.net/npm/vue@2" )
script( src="https://unpkg.com/vuex@3" )
  
.b-check-detail-sign.ph-block

  .ph
    div
    div
    div
    div
    div

  .ph-content

    #checkDetailSign
  
// end /components/check.detail.sign/